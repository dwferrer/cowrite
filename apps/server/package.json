{
  "name": "@cowrite/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Cowrite backend: local console app serving the web UI and the agent harness.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cowrite/shared": "workspace:*",
    "@fastify/static": "^8.0.0",
    "fastify": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
